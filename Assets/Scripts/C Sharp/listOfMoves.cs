/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class listOfMoves : MonoBehaviour
{

	void Start()
	{
		List<moveList> movelist = new List<moveList>();

		movelist.Add(new moveList("Punch", 20, true));
		movelist.Add(new moveList("Slap", 10, true));
		movelist.Add(new moveList("Rush", 30, true));
		movelist.Add(new moveList("Kick", 15, true));

		movelist.Sort();

		foreach (moveList move in movelist)
		{
			Debug.Log(move.NAME + " " + move.DMG);
		}
		//movelist.Clear();

	}

}*/